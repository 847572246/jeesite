<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.wrongselect.dao.WrongSelectionDao">
	
	<!-- 查询数据
	<select id="findList" resultType="WrongSelection">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="finduserwrong" resultType="string">
	SELECT user_name,wrong_reason,flag
	FROM wrong_selection
	WHERE status=0 and flag>0 and user_code=#{usercode}
	</select>
	
</mapper>