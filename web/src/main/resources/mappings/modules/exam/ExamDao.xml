<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.exam.dao.ExamDao">
	
	<!-- 查询数据
	<select id="findList" resultType="Single">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="getsingle" resultType="Single">
		select * 
		from single_selection 
		where status=0
		order by rand() limit 5
	</select>

	<select id="getmultiple" resultType="Multiple">
		select * 
		from multiple_selection
		where status=0
		order by rand() limit 5
	</select>
	
	<select id="findsingle" resultType="Single">
		select * 
		from single_selection 
		where status=0 and id=#{id}
	</select>
		<select id="findmultiple" resultType="Multiple">
		select * 
		from multiple_selection 
		where status=0 and id=#{id}
	</select>
</mapper>