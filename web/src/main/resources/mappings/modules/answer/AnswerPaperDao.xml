<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.answer.dao.AnswerPaperDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AnswerPaper">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="findexamsingleid" resultType="String">
		select sin_question_id
		from examination_paper
		where status=0 and id=#{id}
	</select>
	<select id="findexammultipleid" resultType="String">
		select mul_question_id
		from examination_paper
		where status=0 and id=#{id}
	</select>
	<select id="findexamsingle" resultType="SingleSelection">
		select *
		from single_selection
		where status=0 and instr(#{ids,jdbcType=VARCHAR}, id)
	</select>
	<select id="findexammultiple" resultType="MultipleSelection">
		select *
		from multiple_selection
		where status=0 and instr(#{ids,jdbcType=VARCHAR}, id)
	</select>
	<!--将试卷状态改为已答-->
	<select id="changpaperstatusone" resultType="String">
		update answer_paper
		set paper_status= 1
		where id=#{answerPaperid}
	</select>
	<!--将试卷状态改为已确认-->
	<select id="changpaperstatustwo" resultType="String">
		update answer_paper
		set paper_status= 2
		where id=#{answerPaperid}
	</select>
	<select id="findanswersingle" resultType="String">
		select sin_question_id
		from answer_paper
		where status=0 and id=#{id}
	</select>
	<select id="findanswermultiple" resultType="String">
		select mul_question_id
		from answer_paper
		where status=0 and id=#{id}
	</select>
	<select id="findanswer" resultType="PaperSelection">
		select *
		from paper_selection
		where status=0 and paper_id=#{id}
	</select>
	<select id="findusername" resultType="String">
		select user_name
		from js_sys_user
		where status=0 and user_code=#{usercode}
	</select>
	<!--根据试卷号查找答卷状态-->
	<select id="findpaperstatus" resultType="String">
		select paper_status
		from answer_paper
		where status=0 and id=#{id}
	</select>
</mapper>