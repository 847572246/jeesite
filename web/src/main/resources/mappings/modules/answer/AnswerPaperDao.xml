<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.answer.dao.AnswerPaperDao">
	
	<!-- 查询数据
	<select id="findList" resultType="AnswerPaper">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="findexamsingleid" resultType="String">
		select sin_question_id
		from examination_paper
		where status=0 and id=#{id}
	</select>
	<select id="findexammultipleid" resultType="String">
		select mul_question_id
		from examination_paper
		where status=0 and id=#{id}
	</select>
	<select id="findexamsingle" resultType="SingleSelection">
		select *
		from single_selection
		where status=0 and instr(#{ids,jdbcType=VARCHAR}, id)
	</select>
	<select id="findexammultiple" resultType="MultipleSelection">
		select *
		from multiple_selection
		where status=0 and instr(#{ids,jdbcType=VARCHAR}, id)
	</select>
	<select id="changpaperstatus" resultType="String">
		update answer_paper
		set paper_status= 1
		where id=#{answerPaperid}
	</select>
</mapper>